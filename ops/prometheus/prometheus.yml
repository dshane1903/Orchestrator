global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: forgeflow
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - forgeflow:8080
