forgeflow:
  application:
    name: forgeflow
    version: 0.1.0-SNAPSHOT

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true

server:
  port: 8080
